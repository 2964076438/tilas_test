<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.mapper.ClazzMapper">
    <insert id="insert">
        insert into clazz(name,
                          room,
                          begin_date,
                          end_date,
                          master_id,
                          subject,
                          create_time,
                          update_time)
        values(#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update clazz set name = #{name},
                         room = #{room},
                         begin_date = #{beginDate},
                         end_date = #{endDate},
                         master_id = #{masterId},
                         subject = #{subject},
                         update_time = #{updateTime}
        where id = #{id}
    </update>
    <delete id="deleteById">
        delete from clazz where id = #{id}
    </delete>
    <select id="pageSelect" resultType="com.demo.pojo.Clazz">
        select c.* from clazz c
        <where>
            <if test="name != null and name != ''">
                and c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null">
                <![CDATA[ and c.end_date >= #{begin} ]]>
            </if>
            <if test="end != null">
                <![CDATA[ and c.begin_date < date_add(#{end}, interval 1 day) ]]>
            </if>


        </where>
        </select>
    <select id="selectById" resultType="com.demo.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, subject, create_time, update_time
        from clazz where id = #{id}
    </select>
</mapper>