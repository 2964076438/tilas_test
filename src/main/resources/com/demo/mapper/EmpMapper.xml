<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.EmpMapper">
    <update id="updateById">
        update emp set
                       name=#{name},
                       gender=#{gender},
                       username=#{username},
                       password=#{password},
                       phone=#{phone},job=#{job},
                       salary=#{salary},image=#{image},
                       entry_date=#{entryDate},
                       dept_id=#{deptId},
                       update_time=now() where id=#{id}
    </update>
    <delete id="deleteById">
        delete from emp where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <select id="select" resultType="com.demo.pojo.Emp">
        select emp.*, dept.name as deptName from emp left join dept on emp.dept_id = dept.id
        <where>
            <if test="name != null and name != ''">
                and emp.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and emp.gender=#{gender}
            </if>
            <if test="begin != null and end != null">
                and emp.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by emp.update_time desc
    </select>
    <resultMap id="empResultMap" type="com.demo.pojo.Emp">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="image" property="image"/>
        <result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deptName" property="deptName"/>
        <collection property="exprList" ofType="com.demo.pojo.EmpExpr">
            <id column="e_id" property="id"/>
            <result column="ee_id" property="empId"/>
            <result column="ee_empId" property="job"/>
            <result column="ee_begin" property="begin"/>
            <result column="ee_end" property="end"/>
            <result column="ee_company" property="company"/>
            <result column="ee_job" property="job"/>
        </collection>
    </resultMap>
    <select id="selectById" resultMap="empResultMap">
        #         根据id查询员工信息和员工经历信息，最终封装到Emp对象里
        select e.*,
               ee.id      ee_id,
               ee.emp_id  ee_empId,
               ee.begin   ee_begin,
               ee.end     ee_end,
               ee.company ee_company
                ,
               ee.job     ee_job
        from emp e
                 left join emp_expr ee on e.id = ee.emp_id
        where e.id = #{id}

    </select>
    <select id="selectByUsernameAndPassword" resultType="com.demo.pojo.Emp">
        select id,username,password from emp where username=#{username} and password=#{password}
    </select>

</mapper>
